<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Configuración de la conexión a PostgreSQL -->
        <property name="connection.url">jdbc:postgresql://localhost/futboldb</property>
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.username">postgres</property>
        <property name="connection.password">postgres</property>

        <!-- Configuración de Hibernate -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgresPlusDialect</property>
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>
        <!-- <property name="hibernate.hbm2ddl.auto">create</property> --> <!-- Que se cree la base de datos con las tablas si no existen, pero si se vuelve a ejecutar se borran y se crean de nuevo -->

        <!-- Mapeo de entidades -->
        <mapping class="com.pedro.model.Equipo"/>
        <mapping class="com.pedro.model.Xogador"/>
    </session-factory>
</hibernate-configuration>